export interface Employee {
    first_name: string;
    last_name: string;
    birth_date: string;
    document_type: number; // Cambiar a tipo de dato acorde dependiendo si usamos Enum o tabla aparte
    document_number: string;
    address: string;
    phone_number: string;
    personal_email: string; // Test
  }
  