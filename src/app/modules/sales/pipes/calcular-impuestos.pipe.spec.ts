import { CalcularImpuestosPipe } from './calcular-impuestos.pipe';

describe('CalcularImpuestosPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularImpuestosPipe();
    expect(pipe).toBeTruthy();
  });
});
