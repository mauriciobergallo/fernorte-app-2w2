import { CaseConverterPipe } from './case-converter.pipe';

describe('CaseConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new CaseConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
