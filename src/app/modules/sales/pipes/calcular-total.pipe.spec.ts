import { CalcularTotalPipe } from './calcular-total.pipe';

describe('CalcularTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
