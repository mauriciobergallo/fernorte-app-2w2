import { CalcularMontoTotalPipe } from './calcular-monto-total.pipe';

describe('CalcularMontoTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularMontoTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
